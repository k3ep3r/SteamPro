version: '3.8'
services:
  api:
    image: python:3.9
    volumes:
      - .:/app
    working_dir: /app
    command: sh -c "pip install fastapi uvicorn requests && uvicorn backend.main:app --host 0.0.0.0"
    ports:
      - "8000:8000"
